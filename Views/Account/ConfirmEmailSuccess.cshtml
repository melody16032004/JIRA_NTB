@{
    ViewData["Title"] = "Xác nhận email thành công";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}

<div class="flex items-center justify-center min-h-screen py-8">
    <div class="w-full max-w-md px-4">
        <div class="bg-gray-800/50 backdrop-blur-xl rounded-xl shadow-2xl border border-gray-700/50 overflow-hidden">
            
            <!-- Header -->
            <div class="bg-gradient-to-r from-green-600 to-emerald-600 p-6 text-center">
                <div class="flex justify-center mb-3">
                    <div class="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center backdrop-blur-sm">
                        <i data-lucide="check-circle" class="w-10 h-10 text-white"></i>
                    </div>
                </div>
                <h2 class="text-2xl font-bold text-white">Thành công! 🎉</h2>
            </div>

            <!-- Content -->
            <div class="p-6 space-y-4">
                <div class="text-center space-y-2">
                    <p class="text-gray-300 text-base">
                        Email của bạn đã được xác nhận thành công!
                    </p>
                    <p class="text-gray-400 text-sm">
                        Bây giờ bạn có thể đăng nhập vào hệ thống.
                    </p>
                </div>

                <div class="bg-green-500/10 border border-green-500/50 rounded-lg p-3">
                    <div class="flex items-center gap-2">
                        <i data-lucide="shield-check" class="w-5 h-5 text-green-400 flex-shrink-0"></i>
                        <p class="text-green-400 text-sm">
                            Tài khoản đã được kích hoạt!
                        </p>
                    </div>
                </div>

                <!-- Login Button -->
                <a asp-action="Login" 
                   class="w-full bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-semibold py-2.5 px-4 rounded-lg transition-all duration-200 shadow-lg hover:shadow-xl flex items-center justify-center gap-2">
                    <i data-lucide="log-in" class="w-5 h-5"></i>
                    Đăng nhập ngay
                </a>

                <div class="text-center">
                    <a asp-controller="Home" asp-action="Index" 
                       class="inline-flex items-center gap-2 text-gray-400 hover:text-gray-300 text-sm transition-colors">
                        <i data-lucide="home" class="w-4 h-4"></i>
                        Về trang chủ
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            lucide.createIcons();
        });
    </script>
}
