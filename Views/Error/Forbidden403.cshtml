@{
    ViewData["Title"] = "403 - Từ chối Truy cập";
    Layout = null; // Đặt là null để đè lên layout chính
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <link rel="stylesheet" href="/css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700;900&display=swap" rel="stylesheet" />
    <style>
        body {
            font-family: 'Be Vietnam Pro', sans-serif;
        }
    </style>
</head>
<body class="bg-gradient-to-b from-gray-950 to-gray-900 flex items-center justify-center min-h-screen p-4 text-gray-200">

    <div class="w-full max-w-lg text-center bg-gray-800 p-8 sm:p-12 rounded-2xl shadow-2xl border border-gray-700/50">

        <div class="w-20 h-20 bg-red-900/50 border-4 border-red-800/60 rounded-full flex items-center justify-center mx-auto text-red-500">
            <i data-lucide="shield-alert" class="w-10 h-10"></i>
        </div>

        <h1 class="mt-8 text-7xl font-bold text-red-600">403</h1>

        <h2 class="mt-4 text-2xl sm:text-3xl font-bold text-white">
            Từ chối Truy cập
        </h2>

        <p class="mt-4 text-gray-400 text-sm sm:text-base">
            Xin lỗi, bạn không có quyền truy cập vào tài nguyên này. Vui lòng đăng nhập tài khoản hoặc liên hệ quản trị viên nếu bạn tin rằng đây là một lỗi.
        </p>

        <div class="mt-8 flex flex-col sm:flex-row items-center justify-center gap-4">
            <a href="/"
               class="w-full sm:w-auto inline-flex items-center justify-center gap-2 rounded-lg bg-indigo-600 px-6 py-3 text-sm font-medium text-white hover:bg-indigo-700 transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 focus:ring-offset-gray-800 shadow-lg hover:shadow-indigo-500/30">
                <i data-lucide="home" class="w-4 h-4"></i>
                Quay về Trang chủ
            </a>
        </div>
    </div>

    <script>
        lucide.createIcons();
    </script>

</body>
</html>